let myLibrary = [];

function book(title, author, pages, readStatus) {
  this.title = author;
  this.author = marker;
  this.pages = pages;
  this.readStatus = readstatus;
}

function addBookToLibrary() {
  // do stuff here
}